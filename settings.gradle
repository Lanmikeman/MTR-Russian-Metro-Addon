pluginManagement {
    repositories {
        maven { url = "https://maven.fabricmc.net/" }
        maven { url "https://maven.minecraftforge.net/" }
        gradlePluginPortal()
    }
}

plugins {
    id "org.gradle.toolchains.foojay-resolver-convention" version "1.0.0"
}

// Port 26.1.2: Fabric only for now
include("fabric")
// include("forge")

rootProject.name = "MTR-Russian-Metro-Addon"
